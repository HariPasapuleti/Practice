#include <bits/stdc++.h>

using namespace std;


int main()
{
    int arr[] = {4, 2, 8, 6, 15, 5, 9, 20};
    int n = sizeof(arr) / sizeof(arr[0]);
    cout << sizeof(arr) << " " << sizeof(arr[0]);
    return 0;
}