#include <iostream>
#include <vector>
using namespace std;

int distinctDigitArray(vector<int> nums)
{
    int n = nums.size();
    int count = 0;
    for (int i = 0; i < n; i++)
    {
        for(int j = 0; j < n; j++)
        {
            
        }

    }
}

int main() {
    vector<int> nums = {131, 11, 48};
    distinctDigitArray(nums);
}